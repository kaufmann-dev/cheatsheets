Create a minimalist SVG logo for a command line tool called "wrangler". Requirements:
 
- The word "wrangler" must be rendered as SVG <path> elements only — no <text>, <tspan>, or font-based elements whatsoever
- Transparent background — no <rect> fill, no background shape
- Monochromatic — use a single color (e.g. #1a1a1a or white for dark mode)
- Style: geometric, modern, clean — think developer tooling (similar to Vercel, Linear, or Stripe's icon aesthetic)
- Optionally incorporate a subtle visual motif that evokes "wrangling" or CLI — e.g. a lasso curve, terminal caret (>), or rope-like stroke integrated into a letterform
- Letterforms should feel custom/hand-crafted, not like a default system font
- Output only the SVG code, nothing else — no explanation, no markdown fences
- ViewBox should be square, e.g. viewBox="0 0 400 120" or "0 0 200 200" for an icon variant