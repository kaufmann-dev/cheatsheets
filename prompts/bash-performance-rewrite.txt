You are a Bash performance expert.

Analyze all shell scripts in this repository and rewrite them to be as fast as possible.

Goal:
Optimize the runtime performance of all Git Wrangler shell scripts while maintaining cross-platform compatibility and respecting non-performance-related project rules.

Rules:
- the scripts must still work across macOS, Linux, and Windows (via Git Bash / WSL)
- adhere strictly to all rules in `AGENTS.md` that do NOT affect performance (e.g., naming conventions, headers)
- override `AGENTS.md` rules only if they explicitly hurt performance (e.g., subshell isolation during large loops)
- do NOT use GNU-only flags (e.g., `grep -P`, `sed -i` without extension arg)
- use only POSIX `awk` (no GNU `gawk` extensions)
- do NOT access the `/proc` filesystem
- do NOT use `mapfile` / `readarray` unless bash 4+ is already required
- use forward slashes for paths and use `command -v` to locate tools instead of hardcoding paths
- default parallel jobs to a self-tuning count based on `nproc` or `sysctl -n hw.ncpu`
- preserve all original functionality and exact output formats

Process:
1. Discover and list all relevant shell scripts (e.g., the root `wrangler` dispatcher and `libexec/wrangler-*` scripts).
2. Read `AGENTS.md` and explicitly list which rules you are keeping and which you are overriding for performance.
3. Audit every script for performance issues: unnecessary forks, useless external commands, loop inefficiencies, pipeline overhead, and I/O inefficiency.
4. Rewrite every script, applying optimizations (e.g., replacing `$(cat file)` with redirection, batching `grep`/`awk`, caching `$(pwd)`).
5. Add a short comment on each changed line: `# PERF: <reason>`.
6. Update `AGENTS.md` to document any new performance-first best practices.
7. Generate a consolidated table summarizing all changes, original vs replacement patterns, and estimated speedups.

Output:
Provide the rewritten scripts, the `AGENTS.md` updates, and the final change summary table.

Do not change anything that does not affect performance or violate an applicable rule.
Do not use non-POSIX extensions that break on macOS.